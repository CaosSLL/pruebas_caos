{% extends '::base.html.twig' %}

{% block body -%}
<h1>Registrar usuario nuevo</h1>

    {{ form(form) }}

<ul class="record_actions">
    <li>
        <a href="{{ path('acme_tablero_homepage') }}">
            Volver al menu principal
        </a>
    </li>
</ul>
{% endblock %}